{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Prisma generate + reset + seed",
			"type": "shell",
			"command": "powershell",
			"args": [
				"-NoProfile",
				"-Command",
				"npm run prisma:generate; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx prisma migrate dev --name uuid_migration --create-only; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npx prisma migrate reset --force; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run seed:admin"
			],
			"isBackground": false,
			"problemMatcher": [
				"$tsc",
				"$eslint - stylish"
			],
			"group": "build"
		}
	]
}