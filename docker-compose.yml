services:
  postgres:
    image: postgres:16-alpine
    container_name: hepibite-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # pgadmin:
  #   image: dpage/pgadmin4:8
  #   container_name: hepibite-pgadmin
  #   restart: unless-stopped
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL:-admin@local}
  #     PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD:-admin}
  #   ports:
  #     - "5050:80"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  whatsapp:
    image: aldinokemal2104/go-whatsapp-web-multidevice
    # avoid conflicting with an existing container named "whatsapp"
    container_name: hepibite-whatsapp
    restart: always
    ports:
      - "3002:3002"
    volumes:
      - whatsapp:/app/storages
    environment:
      APP_BASIC_AUTH : ${GOWA_ADMIN}:${GOWA_PASSWORD}
      APP_PORT : ${GOWA_PORT}
      APP_DEBUG : true
      APP_OS : Chrome
      APP_ACCOUNT_VALIDATION : false

  minio:
    image: minio/minio:RELEASE.2025-04-22T22-12-26Z
    volumes:
      - minio_data:/data
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
      - MINIO_BROWSER_REDIRECT_URL=${MINIO_BROWSER_REDIRECT_URL}
    command: server /data --console-address ":9001"
    ports:
      - 9000:9000
    expose:
      - 9001

volumes:
  db_data:
  minio_data:
  whatsapp:
